# `zero_files` — referentie (demo)

De map **`camera_project/`** bevat de **eerdere MQTT-demo** (Picamera2 + AprilTag + broker).

**Actieve missie-code** voor camera + logging + AprilTag staat in **`scripts/camera/`** (zie `scripts/camera/README.md`): SD-log, optionele exposure-bracket, **geen** MQTT.

Deze bestanden blijven als **referentie** voor leerlingen.
